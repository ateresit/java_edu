package ru.geekbrains;

import java.util.HashSet;
import java.util.Map;
import java.util.Set;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.atomic.AtomicLong;

public class NotebookRepo {
/*
    private final Map<Long, Notebook> notebookMap = new ConcurrentHashMap<>();
    private final AtomicLong idetity = new AtomicLong(0);
//    String[] brandName = new String[] {"Lenuvo", "Asos", "MacNote", "Eser", "Xamiou"};

    public void insert(Notebook notebook) {
        long id = idetity.incrementAndGet();
        notebook.setId(id);
        notebookMap.put(id, notebook);
    }
*/


}
